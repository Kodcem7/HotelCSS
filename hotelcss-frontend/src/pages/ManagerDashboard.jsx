import AdminDashboard from './AdminDashboard';

// Manager dashboard uses the same layout and stats as the Admin dashboard
const ManagerDashboard = () => {
  return <AdminDashboard />;
};

export default ManagerDashboard;
